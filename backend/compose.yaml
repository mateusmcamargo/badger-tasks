services:
  postgres:
    image: postgres:16-alpine
    container_name: badger-tasks-db
    environment:
      POSTGRES_DB: badger_tasks
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: